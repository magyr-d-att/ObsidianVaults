## Static design:
Uses fixed time intervals, and with a round robin algorithm every computer gets to talk when it's slot is coming up.

## Dynamic design:

- Centralized: There is one base entity that decides who goes next, by accepting packets and prioritizing based on an algorithm.
- Decentralized: Each machine must decide itself whether to transmit or not.